 <header class="bg-light shadow-sm navbar-sticky">
      <div class="navbar navbar-expand-md navbar-light">
        <div class="container-fluid">
            <a class="navbar-brand  d-sm-block me-4 order-lg-1" href="{{ path('app_home') }}">
                Mon super boutique
            </a>
          <div class="navbar-toolbar d-flex align-items-center order-lg-3">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                <span class="navbar-toggler-icon"></span>
            </button>
          </div>
          <div class="collapse navbar-collapse me-auto order-lg-2" id="navbarCollapse">
                <hr class="my-3">
                <!-- Primary menu-->
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}">Produits</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}">Panier</a>
                    </li>
                    {% if app.user %}
                      <li class="nav-item">
                          <a class="nav-link" href="{{ path('product_index') }}">Gestion des produits</a>
                      </li>
                    {% else %}
                      <li class="nav-item" style="position: absolute; right: 20px;">
                          <a class="nav-link" href="{{ path('product_index') }}"><i class="fa fa-setting"></i> Admin</a>
                      </li>
                    {% endif %}
                </ul>
          </div>
        </div>
      </div>
</header>